import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent wywołuje AppRegistry.registerComponent
// i zapewnia poprawne działanie środowiska Expo w wersji native i web
registerRootComponent(App);
